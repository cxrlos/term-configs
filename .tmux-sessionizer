#!/usr/bin/env bash

tmux rename-window -t "$TMUX_SESSION" 'editor'
tmux send-keys -t "$TMUX_SESSION:editor" 'nvim .' Enter

tmux new-window -t "$TMUX_SESSION" -n 'claude' -c /Users/carlos.garcia/Documents/configs/term-configs
tmux send-keys -t "$TMUX_SESSION:claude" 'claude --ide' Enter

tmux new-window -t "$TMUX_SESSION" -n 'shell' -c /Users/carlos.garcia/Documents/configs/term-configs

tmux select-window -t "$TMUX_SESSION:editor"
