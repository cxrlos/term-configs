# ── Prefix ────────────────────────────────────────────────────────────────────
set -g prefix `
unbind C-b
bind ` send-prefix

# ── Terminal ──────────────────────────────────────────────────────────────────
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -g escape-time 0
set -g focus-events on
set -g repeat-time 800

# ── General ───────────────────────────────────────────────────────────────────
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g detach-on-destroy off
set -g mouse on
set -g history-limit 50000
set -g mode-keys vi
set -g clock-mode-style 24

# ── Pane styling ─────────────────────────────────────────────────────────────
# bg=default lets Alacritty's translucency show through tmux (glass panes).
# Active pane is distinguished by its border + the top command pill, not bg.
set -g window-style "bg=default"
set -g window-active-style "bg=default"
set -g pane-border-style "fg=#11111b bg=#11111b"
set -g pane-active-border-style "fg=#89dceb bg=#1e1e2e"
set -g pane-border-lines heavy
set -g pane-border-indicators colour
set -g pane-border-status top
setw -g pane-border-format "\
#{?pane_active,\
#[fg=#1e1e2e,bg=#89dceb,bold] #{pane_current_command} #[fg=#89dceb,bg=#181825]▸#[fg=#6c7086,bg=#1e1e2e] #{=30:pane_current_path},\
#[fg=#45475a,bg=#11111b] #{pane_current_command}  #{=20:pane_current_path}\
} "

# ── Status bar ────────────────────────────────────────────────────────────────
set -g status-position bottom
set -g status-interval 5
set -g status-style "bg=default,fg=#a6adc8"
set -g status 2
set -g status-format[1] "#[bg=default] "

set -g status-left "#[fg=#89dceb,bg=default]#[fg=#1e1e2e,bg=#89dceb,bold] #S #[fg=#89dceb,bg=default] "
set -g status-left-length 30

# Claude agents in OTHER sessions: gold pill = waiting on you, foam pill = done
# Pills are click-to-jump: range=user|<short session id> (15-byte cap) feeds
# the MouseDown1Status binding below via mouse_status_range
set -g status-right "\
#{S:#{?#{==:#{session_name},#{client_session}},,#{W:#{?#{==:#{@claude_state},waiting},#[range=user|#{session_id}:#{window_index}]#[fg=#1e1e2e]#[bg=#f9e2af] #{session_name}:#{window_index} ◆ #[default]#[norange] ,}#{?#{==:#{@claude_state},done},#[range=user|#{session_id}:#{window_index}]#[fg=#1e1e2e]#[bg=#89dceb] #{session_name}:#{window_index} ● #[default]#[norange] ,}}}}\
#{?window_zoomed_flag,#[fg=#f9e2af]  ,}\
#[fg=#89dceb] #(git -C #{pane_current_path} rev-parse --abbrev-ref HEAD 2>/dev/null) \
#[fg=#45475a]│ \
#(cat $HOME/.cache/zsh_bg_jobs 2>/dev/null)\
#[fg=#a6adc8]󰥔 %H:%M "
set -g status-right-length 150

# Click a Claude pill to jump to that agent's window; anywhere else on the
# window list keeps the default select-window behavior. switch-client doesn't
# format-expand -t, so the jump goes through run-shell -C (which does)
bind -n MouseDown1Status if -F '#{m:*:*,#{mouse_status_range}}' { run-shell -C "switch-client -c '#{client_name}' -t '#{mouse_status_range}'" } { select-window -t = }

# Claude state glyph per window (set by scripts/claude-tmux-state via hooks)
claude_glyph="#{?#{==:#{@claude_state},waiting},#[fg=#f9e2af]◆ ,#{?#{==:#{@claude_state},done},#[fg=#89dceb]● ,}}"
setw -g window-status-format "#[fg=#6c7086] #I #W $claude_glyph#[default]"
setw -g window-status-current-format "#[fg=#89dceb,bg=default]#[fg=#1e1e2e,bg=#89dceb,bold] #I #W $claude_glyph#[fg=#89dceb,bg=default]#[default]"
setw -g window-status-separator " "

# ── Messages, menus & copy mode ──────────────────────────────────────────────
set -g message-style "fg=#cdd6f4,bg=#313244"
set -g mode-style "fg=#1e1e2e,bg=#89dceb"
set -g display-time 2000

# ── Popup styling ────────────────────────────────────────────────────────────
set -g popup-border-style "fg=#89dceb"
set -g popup-border-lines rounded

# ── Seamless vim-tmux navigation ─────────────────────────────────────────────
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"

# ── Sessions ──────────────────────────────────────────────────────────────────
bind Tab switch-client -l
bind BTab display-popup -E -w 60% -h 45% -T " sessions " "$HOME/.local/bin/tmux-sessions"
bind Q confirm-before -p "kill session #S? (y/n) " kill-session

# ── Windows ───────────────────────────────────────────────────────────────────
bind c new-window -c "#{pane_current_path}"

bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5
bind 6 select-window -t 6
bind 7 select-window -t 7
bind 8 select-window -t 8
bind 9 select-window -t 9

bind -r n next-window
bind -r b previous-window
bind ^ last-window
bind z resize-pane -Z
bind < swap-window -d -t -1
bind > swap-window -d -t +1

# ── Panes ─────────────────────────────────────────────────────────────────────
bind v split-window -h -c "#{pane_current_path}"
bind s split-window -v -c "#{pane_current_path}"

bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

bind -r H resize-pane -L 3
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 3

bind x kill-pane
bind X kill-window

# ── Clipboard (platform-aware) ───────────────────────────────────────────────
# All copies go through @clip_copy below; OSC 52 forwarding would duplicate the
# write and trips Alacritty's flaky NSPasteboard path (alacritty/alacritty#8041)
set -g set-clipboard off

if-shell 'test "$(uname)" = "Darwin"' {
    set -g @clip_copy  "pbcopy"
    set -g @clip_paste "pbpaste"
} {
    if-shell 'test "${XDG_SESSION_TYPE:-}" = "wayland"' {
        set -g @clip_copy  "wl-copy"
        set -g @clip_paste "wl-paste"
    } {
        set -g @clip_copy  "xclip -selection clipboard"
        set -g @clip_paste "xclip -selection clipboard -o"
    }
}

# ── Copy mode (vi) ────────────────────────────────────────────────────────────
bind Enter copy-mode
bind P paste-buffer
bind -T copy-mode-vi v     send -X begin-selection
bind -T copy-mode-vi C-v   send -X rectangle-toggle
bind -T copy-mode-vi y     send -X copy-pipe-and-cancel "#{@clip_copy}"
bind -T copy-mode-vi Y     send -X copy-line \; run "tmux save-buffer - | #{@clip_copy}"
bind -T copy-mode-vi C-u   send -X halfpage-up
bind -T copy-mode-vi C-d   send -X halfpage-down
bind -T copy-mode-vi Escape send -X cancel
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "#{@clip_copy}"

# ── Floating popups ──────────────────────────────────────────────────────────
bind ? display-popup -E -w 52 -h 80% -T " tmux keys "  "$HOME/.tmux-cheatsheet.sh"
bind g display-popup -E -w 90% -h 90% "lazygit"
bind m display-popup -E -w 80% -h 80% "htop"
bind f display-popup -E -w 60 -h 20 -T " projects " "$HOME/.local/bin/tmux-sessionizer"
bind p display-popup -E -w 60 -h 20 -T " projects " "$HOME/.local/bin/tmux-sessionizer --initiatives"
bind t display-popup -E -w 80% -h 80% -T " #{b:pane_current_path} " -d "#{pane_current_path}" -e TERM_FLOAT=1 "$SHELL"

# ── Plugins (TPM) ────────────────────────────────────────────────────────────
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'laktak/extrakto'

set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @thumbs-key Space
set -g @tmux-fzf-launch-key 'w'
set -g @extrakto_key 'e'
set -g @extrakto_split_direction 'p'
set -g @extrakto_fzf_tool 'fzf'

# ── Reload ────────────────────────────────────────────────────────────────────
bind r source-file ~/.tmux.conf \; display "Config reloaded"

if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"
run '~/.tmux/plugins/tpm/tpm'
